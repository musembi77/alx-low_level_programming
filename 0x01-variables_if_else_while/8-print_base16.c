#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <time.h>

/**
 * main -Entry
 *
 * description - prints digits of bases 16 from 0
 *
 * Return : Always (0)Success
 *
 * */


int main(void)
{
	int n,x;

	for (n = 0; n <= 9; n++)
		putchar(n);
	for (x = a; x <= f; x++)
		putchar(x);
			putchar("\n");
			return (0);
}
